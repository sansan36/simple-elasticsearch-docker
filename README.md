# simple-elasticsearch-docker

You can use this to run elasticsearch for testing in your local machine, or for use on your simple server.

How to run:

    $ docker-compose up -d

How to stop:

    $ docker-compose down
